import React from 'react';

import Header from 'src/components/Header';

import './forbidden.sass';

const Forbidden = () => (
  <div id="forbidden">
    <p>Accès refusé, vous n'avez pas les droits pour accéder à cette page.</p>
  </div>
);

export default Forbidden;
